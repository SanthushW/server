# Example environment variables
# Copy to .env and fill in secure values for production
PORT=3000
NODE_ENV=development
# Set a strong JWT secret in production. Do NOT use 'dev_secret'.
JWT_SECRET=replace_with_a_strong_random_value
